@model IEnumerable<dance_club.Models.Employees_Titles>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employees.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Titles.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employees.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Titles.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmployeeTitleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmployeeTitleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmployeeTitleID })
        </td>
    </tr>
}

</table>
